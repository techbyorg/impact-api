type Metric {
  id: ID!
  orgId: ID!
  slug: String!
  name: String
  unit: String
}

type MetricConnection {
  nodes: [Metric!]
  totalCount: Int
}

extend type Block {
  metrics: MetricConnection
}
