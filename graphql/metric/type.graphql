type Metric {
  id: ID!
  orgId: ID!
  slug: String!
  name: String
  unit: String
  firstDatapointTime: Date
}

type MetricConnection {
  nodes: [Metric!]
  totalCount: Int
}

extend type Query {
  metrics: MetricConnection
}

extend type Block {
  metrics: MetricConnection
}
