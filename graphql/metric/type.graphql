type Metric {
  id: ID!
  teamId: ID!
  name: String
}

type MetricConnection {
  nodes: [Metric!]
  totalCount: Int
}

extend type Query {
  metrics(teamId: String, startDate: String, endDate: String, timeScale: String): MetricConnection
}

extend type Block {
  metrics: Metric
}
