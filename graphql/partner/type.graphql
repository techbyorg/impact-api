type Partner {
  id: ID!
  slug: String!
  name: String
  segmentId: ID
}

type PartnerConnection {
  nodes: [Partner!]
  totalCount: Int
}

extend type Query {
  partners(orgId: ID!, hackPw: String): PartnerConnection # FIXME: rm hackPw
  partner(orgId: ID!, slug: String): Partner
}
