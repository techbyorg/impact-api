type Segment {
  id: ID!
  orgId: ID!
  slug: String!
  name: String
}

type SegmentConnection {
  nodes: [Segment!]
  totalCount: Int
}

extend type Query {
  segment(slug: String!): Segment
  segments: SegmentConnection
}
