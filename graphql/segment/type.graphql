type Segment {
  id: ID!
  orgId: ID!
  slug: String!
  name: String
}

type SegmentConnection {
  nodes: [Segment!]
  totalCount: Int
}

extend type Query {
  # FIXME: rm hackPw when internal dashboard
  segments(orgId: ID!, hackPw: String): SegmentConnection
}
