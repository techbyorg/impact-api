type Datapoint {
  id: ID!
  scaledTime: String!
  dimensionSlug: String
  dimensionValue: String
  count: Int
}

type DatapointConnection {
  nodes: [Datapoint!]
  totalCount: Int
}

extend type Dimension {
  datapoints(startDate: String, endDate: String, timeScale: String): DatapointConnection
}
